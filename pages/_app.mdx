import "../styles/global.css";
import "@radix-ui/themes/styles.css";

import { DM_Sans, DM_Serif_Display } from "next/font/google";
import { Theme } from "@radix-ui/themes";

export const dm_sans = DM_Sans({
  subsets: ["latin"],
});

export const dm_serif_display = DM_Serif_Display({
  subsets: ["latin"],
  weight: "400",
});

export default function CanopyDocsApp({ Component, pageProps }) {
  return (
    <Theme className={dm_sans.className}>
      <style jsx global>{`
        :root {
          --shiki-color-text: var(--slate-10);
          --shiki-color-background: transparent;
          --shiki-token-constant: var(--indigo-9);
          --shiki-token-string: var(--grass-11);
          --shiki-token-comment: var(--slate-11);
          --shiki-token-keyword: var(--crimson-11);
          --shiki-token-parameter: var(--gold-11);
          --shiki-token-function: var(--violet-11);
          --shiki-token-string-expression: var(--indigo-11);
          --shiki-token-punctuation: var(--slate-8);
          --shiki-token-link: var(--grass-11);
        }
        html {
          --canopy-sans-font: ${dm_sans.style.fontFamily};
          --canopy-display-font: ${dm_serif_display.style.fontFamily};
        }
        .radix-themes {
          --default-font-family: var(--canopy-sans-font), sans-serif;
        }
      `}</style>
      <Component {...pageProps} />
    </Theme>
  );
}
